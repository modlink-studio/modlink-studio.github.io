import { _ as _export_sfc, o as openBlock, c as createElementBlock, ag as createStaticVNode } from "./chunks/framework.DnjriQqU.js";
const __pageData = JSON.parse('{"title":"UI 模块架构","description":"","frontmatter":{},"headers":[],"relativePath":"ui.md","filePath":"ui.md","lastUpdated":1774187593000}');
const _sfc_main = { name: "ui.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="ui-模块架构" tabindex="-1">UI 模块架构 <a class="header-anchor" href="#ui-模块架构" aria-label="Permalink to &quot;UI 模块架构&quot;">​</a></h1><p>UI 层面对的是“已经进入系统的流”，而不是“设备底层协议”。</p><p>这页最重要的一句话是：</p><ul><li>UI 应该依赖 <code>StreamDescriptor</code> 和 <code>FrameEnvelope</code>，不要直接依赖某个具体设备的连接细节</li></ul><h2 id="ui-最应该关心什么" tabindex="-1">UI 最应该关心什么 <a class="header-anchor" href="#ui-最应该关心什么" aria-label="Permalink to &quot;UI 最应该关心什么&quot;">​</a></h2><p>写展示页面时，通常最值得先看的是：</p><ul><li><code>stream_id</code></li><li><code>payload_type</code></li><li><code>data.shape</code></li><li><code>channel_names</code></li><li><code>unit</code></li><li><code>display_name</code></li></ul><p>如果这几件事已经稳定，UI 通常就能稳定。</p><h2 id="一个合理的-ui-工作顺序" tabindex="-1">一个合理的 UI 工作顺序 <a class="header-anchor" href="#一个合理的-ui-工作顺序" aria-label="Permalink to &quot;一个合理的 UI 工作顺序&quot;">​</a></h2><p>推荐的顺序是：</p><ol><li>先拿到 <code>StreamDescriptor</code></li><li>再决定这个流应该怎么画</li><li>再订阅对应 <code>stream_id</code></li><li>最后根据 <code>FrameEnvelope.data</code> 的 shape 解释实时数据</li></ol><p>也就是说，UI 不应该在收到 frame 时临时猜“这是不是 EEG”“这是不是三轴加速度计”，这些信息应该尽量来自 descriptor。</p><h2 id="一个-stream-通常对应什么" tabindex="-1">一个 stream 通常对应什么 <a class="header-anchor" href="#一个-stream-通常对应什么" aria-label="Permalink to &quot;一个 stream 通常对应什么&quot;">​</a></h2><p>在当前项目里，一个 stream 通常对应一个“稳定的展示面板”。</p><p>例如：</p><ul><li>一个 EEG stream 对应一个多通道波形面板</li><li>一个加速度计 stream 对应一个三轴面板</li><li>一个音频波形 stream 对应一个波形视图</li></ul><p>同一个 stream 下的多个 channel，通常应该被理解成：</p><ul><li>同一块数据里的多个通道</li><li>同一个面板里的多条曲线或多个子轨道</li></ul><p>而不是多个完全无关的 stream。</p><h2 id="为什么-ui-不该直接依赖设备协议" tabindex="-1">为什么 UI 不该直接依赖设备协议 <a class="header-anchor" href="#为什么-ui-不该直接依赖设备协议" aria-label="Permalink to &quot;为什么 UI 不该直接依赖设备协议&quot;">​</a></h2><p>因为 UI 一旦直接依赖某个设备 SDK 的特殊字段，后面会很难复用。</p><p>举个例子：</p><ul><li>如果 UI 直接判断“这是 Ganglion，所以应该有 4 个 EEG 通道”</li><li>那它就无法自然复用到另一个 EEG 设备</li></ul><p>更好的方式是：</p><ul><li>UI 只读 <code>StreamDescriptor.channel_names</code></li><li>UI 只读 <code>payload_type</code></li><li>UI 只解释统一的 <code>FrameEnvelope.data</code></li></ul><p>这样换一个设备，只要 descriptor 和 data shape 还守同一套约定，UI 就能继续工作。</p><h2 id="ui-和-core-的边界" tabindex="-1">UI 和 Core 的边界 <a class="header-anchor" href="#ui-和-core-的边界" aria-label="Permalink to &quot;UI 和 Core 的边界&quot;">​</a></h2><p>UI 通常不需要知道：</p><ul><li>driver 跑在哪条线程上</li><li>设备怎么搜索</li><li>设备怎么连接</li><li>流是 callback 还是 loop 驱动的</li></ul><p>UI 真正需要知道的是：</p><ul><li>这个流叫什么</li><li>这个流怎么解释</li><li>什么时候有新数据</li></ul><p>所以对 UI 开发者来说，Core 更像“稳定的流提供者”，而不是“必须深入理解的内部系统”。</p><h2 id="当前最值得依赖的稳定字段" tabindex="-1">当前最值得依赖的稳定字段 <a class="header-anchor" href="#当前最值得依赖的稳定字段" aria-label="Permalink to &quot;当前最值得依赖的稳定字段&quot;">​</a></h2><p>如果你想让页面尽量通用，建议优先依赖这些字段：</p><ul><li><code>descriptor.stream_id</code></li><li><code>descriptor.payload_type</code></li><li><code>descriptor.channel_names</code></li><li><code>descriptor.unit</code></li><li><code>frame.data</code></li></ul><p>其中：</p><ul><li><code>channel_names</code> 和 <code>unit</code> 已经比随意塞到 <code>metadata</code> 里更适合做共享约定</li><li><code>metadata</code> 更适合做补充信息，而不是做 UI 主逻辑的唯一依据</li></ul><h2 id="推荐阅读" tabindex="-1">推荐阅读 <a class="header-anchor" href="#推荐阅读" aria-label="Permalink to &quot;推荐阅读&quot;">​</a></h2><ul><li>接设备的人：看 <a href="/sdk">SDK</a></li><li>看运行时边界的人：看 <a href="/core">Core</a></li><li>看应用如何挂页面的人：看 <a href="/app">App</a></li></ul>', 39)
  ])]);
}
const ui = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  ui as default
};
