import{_ as i,o as a,c as d,ag as o}from"./chunks/framework.BZohXCq9.js";const k=JSON.parse('{"title":"SDK 开发者指南","description":"","frontmatter":{},"headers":[],"relativePath":"sdk.md","filePath":"sdk.md","lastUpdated":1775473802000}'),s={name:"sdk.md"};function l(c,e,t,n,r,p){return a(),d("div",null,[...e[0]||(e[0]=[o(`<h1 id="sdk-开发者指南" tabindex="-1">SDK 开发者指南 <a class="header-anchor" href="#sdk-开发者指南" aria-label="Permalink to &quot;SDK 开发者指南&quot;">​</a></h1><p>这一页记录的是把设备接入 <code>ModLink Studio</code> 时最常用的约定。</p><p>第一版的核心原则只有两条：</p><ul><li>外部 driver 项目优先依赖 <code>modlink-sdk</code> 这一层契约</li><li>安装后通过 <code>modlink.drivers</code> entry point 被宿主发现</li></ul><p>当前文档以 <code>0.2.0</code> 主线为准。<code>0.2.0</code> 不兼容 <code>0.1.x</code> 的 Qt-style driver API：<code>modlink_sdk</code> 已不再要求 <code>QObject</code>、Qt signal 或 <code>QTimer</code>。</p><p>需要特别说明的是：<code>0.2.0</code> 的公开 PyPI 发布面当前收口为 <code>modlink-studio</code> 一个主包；<code>modlink-sdk</code> 这一层契约在仓库中已经稳定存在，但不会作为 <code>0.2.0</code> 的独立公开 PyPI 包同步发布。</p><p>开发前，请先完成宿主环境安装，见 <a href="/install">安装与发布</a>。</p><h2 id="快速定位" tabindex="-1">快速定位 <a class="header-anchor" href="#快速定位" aria-label="Permalink to &quot;快速定位&quot;">​</a></h2><ul><li><a href="#先选哪一个基类">先选哪一个基类</a></li><li><a href="#用脚手架起步">用脚手架起步</a></li><li><a href="#最小接入流程">最小接入流程</a></li><li><a href="#三个核心数据模型">三个核心数据模型</a></li><li><a href="#driver-生命周期">driver 生命周期</a></li><li><a href="#插件项目怎么组织">插件项目怎么组织</a></li><li><a href="#零参数工厂">零参数工厂</a></li><li><a href="#命名建议">命名建议</a></li></ul><h2 id="先选哪一个基类" tabindex="-1">先选哪一个基类 <a class="header-anchor" href="#先选哪一个基类" aria-label="Permalink to &quot;先选哪一个基类&quot;">​</a></h2><h3 id="driver" tabindex="-1"><code>Driver</code> <a class="header-anchor" href="#driver" aria-label="Permalink to &quot;\`Driver\`&quot;">​</a></h3><p><code>Driver</code> 是所有 driver 的根基类。宿主真正理解的是这套基类契约，而不是某个具体设备自己的接入方式。</p><p>一个 <code>Driver</code> 实例通常代表“宿主管理下的一个设备端点”，它需要对外提供：</p><ul><li><code>descriptors()</code>：这个 driver 会发哪些流</li><li><code>search()</code>：当前能找到哪些候选设备</li><li><code>connect_device()</code> / <code>disconnect_device()</code>：连接生命周期</li><li><code>start_streaming()</code> / <code>stop_streaming()</code>：数据流生命周期</li><li><code>bind(context)</code>：接入宿主注入的 <code>DriverContext</code></li><li><code>emit_frame()</code>：向宿主发出 <code>FrameEnvelope</code></li><li><code>emit_connection_lost()</code> / <code>report_error()</code> / <code>set_status()</code>：回传运行时事件</li></ul><p>适合这类设备：</p><ul><li>callback 型 SDK</li><li>第三方库回调驱动的数据源</li><li>很难用固定 <code>loop()</code> 表达清楚的设备</li></ul><p>如果你还不确定设备是不是标准轮询模式，优先先用 <code>Driver</code>。它是最直接的抽象，也更适合在早期先把设备协议跑通。</p><h3 id="drivercontext" tabindex="-1"><code>DriverContext</code> <a class="header-anchor" href="#drivercontext" aria-label="Permalink to &quot;\`DriverContext\`&quot;">​</a></h3><p><code>DriverContext</code> 是宿主在 <code>bind(context)</code> 阶段注入给 driver 的运行时出口。<code>0.2.0</code> 起，driver 与宿主的正式交互通道就是这组 callback，而不是 Qt signal。</p><p>最常用的方法有：</p><ul><li><code>emit_frame(frame)</code>：把 <code>FrameEnvelope</code> 交给宿主</li><li><code>emit_connection_lost(detail)</code>：通知连接丢失</li><li><code>report_error(message)</code>：上报非致命运行时错误</li><li><code>set_status(status, detail=None)</code>：发布 driver 状态</li></ul><p>通常推荐直接使用 <code>Driver</code> 基类已经提供的同名 helper：<code>self.emit_frame(...)</code>、<code>self.emit_connection_lost(...)</code>、<code>self.report_error(...)</code>、<code>self.set_status(...)</code>。</p><h3 id="loopdriver" tabindex="-1"><code>LoopDriver</code> <a class="header-anchor" href="#loopdriver" aria-label="Permalink to &quot;\`LoopDriver\`&quot;">​</a></h3><p><code>LoopDriver</code> 的基类也是 <code>Driver</code>。它不是另一套平行体系，而是在 <code>Driver</code> 之上，为轮询型设备提供的一个已经封装好的 helper。</p><p>它主要适用于这类场景：driver 不需要等待外部 callback，而是要在自己的线程里反复执行一段短操作，例如“读串口缓冲区”“检查 SDK 是否有新样本”“取一批可用数据并发出去”。</p><p><code>LoopDriver</code> 默认已经帮你接好了：</p><ul><li>基于 driver 独立线程的周期调度</li><li><code>start_streaming()</code> / <code>stop_streaming()</code> 的默认实现</li><li><code>on_loop_started()</code> / <code>on_loop_stopped()</code> 这两个可选钩子</li></ul><p>通常你只需要补：</p><ul><li><code>descriptors()</code></li><li><code>search()</code></li><li><code>connect_device()</code> / <code>disconnect_device()</code></li><li><code>loop()</code></li><li>必要时调整 <code>loop_interval_ms</code></li></ul><p>适合这类设备：</p><ul><li>串口轮询</li><li>BrainFlow 风格设备</li><li>一次短循环就能表达的数据获取逻辑</li></ul><p>不适合这类设备：</p><ul><li>callback 型 SDK</li><li>单次读取会长时间阻塞的设备</li><li>采集逻辑本身依赖复杂异步状态机的设备</li></ul><p>如果判断不清楚，回到 <code>Driver</code>。<code>LoopDriver</code> 的目的在于减少轮询样板代码，而不是让所有 driver 都采用轮询模型。</p><h2 id="用脚手架起步" tabindex="-1">用脚手架起步 <a class="header-anchor" href="#用脚手架起步" aria-label="Permalink to &quot;用脚手架起步&quot;">​</a></h2><p><code>modlink-plugin-scaffold</code> 现在作为独立 npm 开发工具维护，位于仓库的 <code>tools/</code> 目录中；它不再作为 <code>modlink-studio</code> 的运行时依赖自动安装。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @modlink-studio/plugin-scaffold</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --zh</span></span></code></pre></div><p>如果你正在仓库里联调脚手架本身：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --workspace</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @modlink-studio/plugin-scaffold</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --zh</span></span></code></pre></div><p>这个脚手架适合“新建一个独立 driver 项目”的场景。它会交互式生成：</p><ul><li>基础包结构</li><li><code>pyproject.toml</code></li><li><code>README.md</code></li><li><code>LICENSE</code></li><li><code>.gitignore</code></li><li><code>&lt;plugin_name&gt;/driver.py</code></li><li><code>&lt;plugin_name&gt;/factory.py</code></li><li><code>modlink.drivers</code> entry point</li><li><code>tests/test_smoke.py</code></li></ul><p>脚手架只负责把项目骨架和 SDK 契约接好，不会替代真实设备协议实现。生成项目后，通常还需要你继续补完：</p><ul><li><code>search()</code></li><li><code>connect_device()</code> / <code>disconnect_device()</code></li><li>真实数据流提供与 <code>FrameEnvelope</code> 发射逻辑</li></ul><h2 id="最小接入流程" tabindex="-1">最小接入流程 <a class="header-anchor" href="#最小接入流程" aria-label="Permalink to &quot;最小接入流程&quot;">​</a></h2><ol><li>定义一个 driver 类</li><li>实现 <code>device_id</code></li><li>实现 <code>descriptors()</code></li><li>实现 <code>search()</code></li><li>实现连接和数据提供逻辑</li><li>通过 <code>emit_frame()</code> 发出 <code>FrameEnvelope</code></li></ol><p>优先先定住两件事：</p><ul><li><code>StreamDescriptor</code></li><li><code>FrameEnvelope.data</code> 的 shape</li></ul><h2 id="三个核心数据模型" tabindex="-1">三个核心数据模型 <a class="header-anchor" href="#三个核心数据模型" aria-label="Permalink to &quot;三个核心数据模型&quot;">​</a></h2><h3 id="searchresult" tabindex="-1"><code>SearchResult</code> <a class="header-anchor" href="#searchresult" aria-label="Permalink to &quot;\`SearchResult\`&quot;">​</a></h3><p><code>SearchResult</code> 是 <code>search()</code> 的返回值。宿主拿它做两件事：</p><ul><li>用 <code>title</code> / <code>subtitle</code> 展示给用户</li><li>在用户选择后，把整个对象回传给 <code>connect_device()</code></li></ul><p>最常用字段：</p><ul><li><code>title</code>：列表主标题</li><li><code>subtitle</code>：列表副标题</li><li><code>extra</code>：driver 自己保存的连接参数</li></ul><p>可选补充字段：</p><ul><li><code>device_id</code>：driver 提供的稳定设备标识</li></ul><p>宿主不会解析 <code>extra</code>。端口号、地址、序列号、驱动私有参数等信息，都可以放在这里。<code>device_id</code> 也不是宿主当前会消费的字段；它不是必填项，只有在 driver 希望额外提供一个规范化设备标识时才需要填写。</p><h3 id="streamdescriptor" tabindex="-1"><code>StreamDescriptor</code> <a class="header-anchor" href="#streamdescriptor" aria-label="Permalink to &quot;\`StreamDescriptor\`&quot;">​</a></h3><p><code>StreamDescriptor</code> 描述“这个 driver 会发哪些流”，它是静态契约，不是实时数据。</p><p>宿主会在连接前就调用 <code>descriptors()</code>，因此这里返回的信息应该在 driver 生命周期内保持稳定。</p><p>先明确一件事：<code>stream_id</code> 不是手写字段，而是由 <code>device_id + modality</code> 自动派生出来的。<br> 因此真正需要你决定的是下面这些字段。</p><h4 id="强约束字段" tabindex="-1">强约束字段 <a class="header-anchor" href="#强约束字段" aria-label="Permalink to &quot;强约束字段&quot;">​</a></h4><p>这些字段不只是补充描述，而是会直接影响宿主如何路由、预览和录制数据。</p><ul><li><code>device_id</code> 这是这个流所属的设备实例标识，必须满足 <code>name.XX</code> 形式，例如 <code>host_camera.01</code>。 它会直接参与 <code>stream_id</code> 的生成。如果这个值变了，系统会把它视为另一个流。</li><li><code>modality</code> 这是流的高层模态标签，例如 <code>eeg</code>、<code>audio</code>、<code>video</code>、<code>accel</code>。 它同样会参与 <code>stream_id</code> 的生成，因此应该使用稳定、可复用的名字，而不是临时描述。</li><li><code>payload_type</code> 这个字段不能任意写，当前只能从这四个值里选： <ul><li><code>signal</code></li><li><code>raster</code></li><li><code>field</code></li><li><code>video</code> 它决定了宿主选用哪一类预览视图、哪一类录制 writer，以及 <code>FrameEnvelope.data</code> 应该满足什么维度约定：</li><li><code>signal</code>：<code>data.shape == [channel_count, chunk_size]</code></li><li><code>raster</code>：<code>data.shape == [channel_count, chunk_size, line_length]</code></li><li><code>field</code>：<code>data.shape == [channel_count, chunk_size, height, width]</code></li><li><code>video</code>：<code>data.shape == [channel_count, chunk_size, height, width]</code> 如果值超出这四个范围，当前预览和录制链路都会直接报错。</li></ul></li><li><code>nominal_sample_rate_hz</code> 这是正数，表示该流的名义采样率。 它会影响预览中的时间轴、部分默认设置和录制时的名义采样周期计算。当前 Core 会要求它是正数，非正值会直接报错。</li><li><code>chunk_size</code> 这是正整数，表示每个 <code>FrameEnvelope</code> 通常打包多少个样本或多少帧。 它不是提示性字段。录制链路会校验运行时收到的实际 <code>chunk_size</code> 是否和 descriptor 一致；如果 descriptor 写的是 32，而实际 frame 发的是 64，当前 writer 会直接报错。 从实践上看，采样率越高、发帧越频繁的设备，通常越适合把 <code>chunk_size</code> 设得更高一些，以减少过于频繁的 frame 派发和处理开销。</li></ul><h4 id="描述性字段" tabindex="-1">描述性字段 <a class="header-anchor" href="#描述性字段" aria-label="Permalink to &quot;描述性字段&quot;">​</a></h4><p>这些字段更偏向“告诉宿主如何展示和解释这个流”，不是路由主键，但仍然建议保持稳定。</p><ul><li><code>channel_names</code> 这是通道标签列表，本质上可以自由命名，例如 <code>(&quot;Fp1&quot;, &quot;Fp2&quot;)</code>、<code>(&quot;x&quot;, &quot;y&quot;, &quot;z&quot;)</code>、<code>(&quot;red&quot;, &quot;green&quot;, &quot;blue&quot;)</code>。 对 <code>signal</code> 类型尤其重要：如果数量和实际 <code>channel_count</code> 一致，预览图和录制文件会使用这些名字；如果数量不一致，系统会退回到自动生成的通道名。</li><li><code>display_name</code> 这是面向用户的可读名称，例如 <code>Ganglion EEG</code>、<code>Host Microphone Waveform</code>。 它主要影响界面显示；如果不提供，UI 通常会退回到 <code>stream_id</code>。</li></ul><h4 id="metadata-应该怎么用" tabindex="-1"><code>metadata</code> 应该怎么用 <a class="header-anchor" href="#metadata-应该怎么用" aria-label="Permalink to &quot;\`metadata\` 应该怎么用&quot;">​</a></h4><p><code>metadata</code> 是补充说明信息，适合放那些“有助于解释流，但又不适合作为主字段”的内容。<br> 它应该保持 JSON 友好，并且在 driver 生命周期内尽量稳定。</p><p>当前最常见、最推荐使用的键是：</p><ul><li><code>unit</code> 表示工程单位，例如 <code>uV</code>、<code>degC</code>、<code>kPa</code>、<code>m/s^2</code>、<code>a.u.</code>。 它并不和 <code>signal</code> 绑定。只要这个流本身有明确量纲，就可以填写 <code>unit</code>；例如信号流、场图流和栅格流都可以使用。 当前系统里，<code>unit</code> 主要有两个作用： <ul><li>会进入录制时保存下来的 descriptor 元数据</li><li>会显示在预览卡片摘要里 目前它不会自动驱动数值缩放、坐标轴换算或滤波参数，因此它更像“明确数据语义”的说明字段，而不是控制行为的配置字段。</li></ul></li></ul><p>其他 payload 相关信息也可以放进 <code>metadata</code>，例如：</p><ul><li><code>length</code></li><li><code>height</code></li><li><code>width</code></li></ul><p>但要注意：当前运行时并不会依赖这些键来决定预览或录制 shape；系统主要还是根据 <code>payload_type</code> 和实际 <code>FrameEnvelope.data.shape</code> 工作。<br> 因此这些键如果提供，应该作为补充说明，并且与真实数据 shape 保持一致，而不是把它们当成唯一真值来源。</p><h3 id="frameenvelope" tabindex="-1"><code>FrameEnvelope</code> <a class="header-anchor" href="#frameenvelope" aria-label="Permalink to &quot;\`FrameEnvelope\`&quot;">​</a></h3><p><code>FrameEnvelope</code> 是运行时真正发出的数据块。它和 <code>StreamDescriptor</code> 的关系必须是一一可解释的：宿主先通过 <code>StreamDescriptor</code> 知道“这个流是什么”，再通过 <code>FrameEnvelope</code> 收到“这个流的实时数据”。</p><p>最重要字段：</p><ul><li><code>stream_id</code>：属于哪个流</li><li><code>timestamp_ns</code>：时间戳</li><li><code>data</code>：数据本体</li><li><code>seq</code>：可选顺序号</li></ul><p>实际接入时最重要的约束有三条：</p><ul><li><code>device_id + modality</code> 会派生出 <code>stream_id</code>，它必须能对应到某个 <code>StreamDescriptor</code></li><li><code>data</code> 的 shape 必须和该流的约定一致</li><li><code>timestamp_ns</code> 需要有真实时间语义，不能只是随手填值</li></ul><h2 id="driver-生命周期" tabindex="-1">driver 生命周期 <a class="header-anchor" href="#driver-生命周期" aria-label="Permalink to &quot;driver 生命周期&quot;">​</a></h2><p>宿主对 driver 的典型调用顺序：</p><ol><li>创建 driver</li><li>读取 <code>device_id</code> / <code>display_name</code></li><li>读取 <code>descriptors()</code></li><li>宿主调用 <code>bind(context)</code></li><li>启动 driver worker thread</li><li>调 <code>on_runtime_started()</code></li><li>调 <code>search()</code></li><li>调 <code>connect_device()</code></li><li>调 <code>start_streaming()</code></li><li>后续调 <code>stop_streaming()</code> / <code>disconnect_device()</code> / <code>shutdown()</code></li></ol><h2 id="插件项目怎么组织" tabindex="-1">插件项目怎么组织 <a class="header-anchor" href="#插件项目怎么组织" aria-label="Permalink to &quot;插件项目怎么组织&quot;">​</a></h2><p>推荐一个 driver 项目一个目录：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>my_driver/</span></span>
<span class="line"><span>├─ pyproject.toml</span></span>
<span class="line"><span>└─ my_driver/</span></span>
<span class="line"><span>   ├─ __init__.py</span></span>
<span class="line"><span>   ├─ factory.py</span></span>
<span class="line"><span>   └─ driver.py</span></span></code></pre></div><p>最小 <code>pyproject.toml</code> 示例：</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">project</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">name = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;my-driver&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">version = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0.2.0&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dependencies = [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;modlink-sdk&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;numpy&gt;=2.3.3&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">project</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">entry-points</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">&quot;modlink</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">drivers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">my-driver = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;my_driver.factory:create_driver&quot;</span></span></code></pre></div><p>这里的边界很重要：</p><ul><li>driver 项目不要直接依赖 <code>modlink-studio</code></li><li>如无必要，不要依赖 <code>modlink-core</code></li><li>宿主只关心 <code>modlink.drivers</code> 和 SDK 契约</li></ul><p><code>factory.py</code> 的职责也应该保持简单：它负责暴露给宿主加载的工厂函数，而不是在这里实现设备协议逻辑。</p><p>当前阶段更推荐在源码或本地联调环境中验证 driver：</p><p>安装方式：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -m</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span></span></code></pre></div><p>安装到与宿主相同的环境后，启动宿主：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -m</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> modlink_studio</span></span></code></pre></div><h2 id="零参数工厂" tabindex="-1">零参数工厂 <a class="header-anchor" href="#零参数工厂" aria-label="Permalink to &quot;零参数工厂&quot;">​</a></h2><p>每个 driver 包都应该通过 <code>modlink.drivers</code> entry point 暴露一个零参数工厂。</p><p>最常见的形式就是：</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> .driver </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MyDriver</span></span>
<span class="line"></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">def</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> create_driver</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() -&gt; MyDriver:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MyDriver()</span></span></code></pre></div><p>这里“零参数”不是风格建议，而是当前宿主的实际契约：</p><ul><li>宿主启动时会扫描 <code>modlink.drivers</code></li><li>entry point 加载出来的对象必须是可调用的</li><li>宿主会直接以无参数形式调用它</li><li>返回值必须是一个 <code>Driver</code> 实例</li></ul><p>因此当前推荐把 entry point 固定写成：</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">project</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">entry-points</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">&quot;modlink</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">drivers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">my-driver = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;my_driver.factory:create_driver&quot;</span></span></code></pre></div><p>这个约束背后的原因很直接：宿主在发现插件时只知道“这里有一个 driver factory”，并不知道你的设备需要什么端口、地址、序列号或认证参数。<br> 这些运行时信息应该放到后续流程里处理：</p><ul><li>设备候选在 <code>search()</code> 里发现</li><li>连接参数通过 <code>SearchResult.extra</code> 回传</li><li>真实连接在 <code>connect_device()</code> 里建立</li></ul><p>因此不建议把这类运行时参数塞进 <code>Driver</code> 构造函数。<br> 构造函数更适合做的是：</p><ul><li>初始化内部状态</li><li>创建还不依赖设备连接的对象</li><li>准备稳定的 descriptor 定义</li></ul><p>而不适合在构造阶段就：</p><ul><li>打开设备</li><li>启动数据流</li><li>依赖必须由用户选择后才能确定的连接参数</li></ul><p>当前宿主还会继续校验工厂返回值：</p><ul><li>返回对象必须是 <code>Driver</code></li><li><code>driver.device_id</code> 不能为空</li></ul><p>所以如果 entry point 不是零参数工厂，或者工厂没有返回一个合法 <code>Driver</code>，宿主会在启动加载阶段直接报错。</p><h2 id="命名建议" tabindex="-1">命名建议 <a class="header-anchor" href="#命名建议" aria-label="Permalink to &quot;命名建议&quot;">​</a></h2><h3 id="device-id" tabindex="-1"><code>device_id</code> <a class="header-anchor" href="#device-id" aria-label="Permalink to &quot;\`device_id\`&quot;">​</a></h3><p>推荐格式：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>name.XX</span></span></code></pre></div><p>例如：</p><ul><li><code>my_driver.01</code></li><li><code>host_camera.01</code></li><li><code>openbci_ganglion.01</code></li></ul><h3 id="stream-id" tabindex="-1"><code>stream_id</code> <a class="header-anchor" href="#stream-id" aria-label="Permalink to &quot;\`stream_id\`&quot;">​</a></h3><p>当前约定由 <code>device_id + modality</code> 自动派生：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{device_id}:{modality}</span></span></code></pre></div><p>例如：</p><ul><li><code>host_camera.01:video</code></li><li><code>openbci_ganglion.01:eeg</code></li></ul><h2 id="官方驱动命名与插件安装" tabindex="-1">官方驱动命名与插件安装 <a class="header-anchor" href="#官方驱动命名与插件安装" aria-label="Permalink to &quot;官方驱动命名与插件安装&quot;">​</a></h2><p>当前仓库内维护的官方驱动使用下面这组 entry point：</p><ul><li><code>host-camera</code></li><li><code>host-microphone</code></li><li><code>openbci-ganglion</code></li></ul><p>在 <code>0.2.x</code> 当前阶段，这些 entry point 主要通过 <code>modlink-plugin install &lt;plugin_id&gt;</code> 安装进当前环境；后续会沿这条路径继续扩展到更通用的插件管理方式。</p>`,127)])])}const u=i(s,[["render",l]]);export{k as __pageData,u as default};
